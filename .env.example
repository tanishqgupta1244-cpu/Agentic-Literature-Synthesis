# Application Environment
APP_ENV=development
DEBUG=true

# Backend
BACKEND_PORT=8000
BACKEND_URL=http://localhost:8000

# Frontend
FRONTEND_PORT=3000
FRONTEND_URL=http://localhost:3000

# Database
DATABASE_URL=postgresql://reviewer:password@localhost:5432/literature_review_dev
DB_HOST=localhost
DB_PORT=5432
DB_NAME=literature_review_dev
DB_USER=reviewer
DB_PASSWORD=your_secure_password_here

# PDF Ingestion (Phase 1)
# Directory where uploaded PDFs are stored (git-ignored).
# Defaults to data/raw when not set.
PDF_STORAGE_DIR=data/raw
# Maximum upload size in megabytes.
MAX_UPLOAD_MB=50

# Optional: pgvector
# Uncomment if pgvector is installed and you want to enable vector search
# PGVECTOR_ENABLED=false

# LangGraph (future phases)
# LANGGRAPH_ENABLED=false

# LLM Configuration (future phases)
# OPENAI_API_KEY=your_api_key_here
# LLM_MODEL=gpt-4
